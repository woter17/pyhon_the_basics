console.log('Вывод текста')
alert('Вывод текста')
document.querySelectorAll('#и сам id') //также для всех др типов даннык к примеру можно найти класс(c . вместо #) или div итд
document.getElementById('сам id') //luche etot
let first = document.getElementById('first'); //sozd i pisv peremennoi
//цикл в js
for (let elem of first) {
    console.log(elem)
//как поменять стили вместо color можно юзать любой другой)
    first.style.color ='red'
}
elem.remove() //удаляет элемент
